@model MediaPortal.Models.FileSystemModels


<div class="col-md-3 fileSystem-list-item" name="@Model.Id" folder-name="@Model.Name">
    
        @if (Model.Type.Equals("Folder"))
        {
            <span id="iconFolder"></span>
        }
        else
        {
            <span id="iconFile"></span>
        }

        @Model.Name
</div>
